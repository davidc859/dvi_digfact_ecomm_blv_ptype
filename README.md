
  # BLV

  This is a code bundle for BLV. The original project is available at https://www.figma.com/design/PJZFlq0clitleqsHYm8ZQR/BLV.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  